% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rebeltrack_measure_impl.R
\name{.rebeltrack_measure_impl}
\alias{.rebeltrack_measure_impl}
\title{Shared implementation for actor/gid measure functions}
\usage{
.rebeltrack_measure_impl(
  x,
  group_col,
  var,
  fill,
  lag,
  weight,
  lag_engine,
  constructor,
  summarise,
  make_group = .simple_group()
)
}
\arguments{
\item{x}{A \code{RebelTrackDataFrame} or \code{RebelTrackDataFrameGid} object}

\item{group_col}{The grouping column for this measure (\code{actor} or \code{priogrid_gid})}

\item{var}{Name of the variable to create (already resolved to a string,
e.g. via \code{rlang::quo_name(rlang::enquo(var))})}

\item{fill}{Default value when no events are observed in the specified period.}

\item{lag}{An integer giving the number of positions to lead or lag by.}

\item{weight}{A weight matrix to apply.}

\item{lag_engine}{\code{weighted_lag} or \code{weighted_lag_gid}}

\item{constructor}{\code{.rebeltrack_dataframe} or \code{.rebeltrack_dataframe_gid}}

\item{summarise}{A function(group) that returns the group summary data
frame via \code{dplyr::summarize(group, .var = value)}, where
\code{value} is this measure's computed column}

\item{make_group}{A function(events, group_col) that returns a tibble
grouped by \code{group_col} and \code{period_start} (after whatever
intermediate mutate/group_by/summarize steps this particular measure
needs), ready to be summarised. Defaults to \code{.simple_group()},
which covers every measure that just groups by group_col and
period_start with no intermediate stage.}
}
\description{
Shared implementation for actor/gid measure functions
}
\keyword{internal}
